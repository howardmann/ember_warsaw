
import { toCurrency } from 'ember-dashboard/helpers/to-currency';
import { module, test } from 'qunit';

module('Unit | Helper | to currency');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toCurrency([42]);
  assert.ok(result);
});

